package main

/*
func main() {
	var L, W, A, B int64
	if _, err := fmt.Scan(&L, &W); err != nil {
		return
	}
	if _, err := fmt.Scan(&A, &B); err != nil {
		return
	}
	ans := (L / A) * (W / B)

	fmt.Println(ans)
}
*/
